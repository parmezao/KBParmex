@model IEnumerable<NPC_Situacao>

@{
    ViewData["Title"] = "JD - KnowledgeBase";
}

    <div style="position:relative; text-align:center">
        <div>
            <img src="~/images/Logo.png" alt="JD Consultores" class="img-thumbnail img-fluid" style="ima" />
            <span style="font-size:30px">JD Base de Conhecimento</span>
        </div>
    </div>
    

@*<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<div>
    <a class="btn btn-outline-secondary" asp-action="Add" asp-controller="Home">Adicionar</a>
    <a class="btn btn-outline-secondary" asp-action="Index" asp-controller="Erros">Erros</a><br />

    <table id="dtBasicExample" class="table table-striped table-hover table-bordered table-sm" cellspacing="0" width="100%">
        <thead class="thead-dark">
            <tr>
                <th>Objeto</th>
                <th>Situação</th>
                <th>Descrição</th>
            </tr>
        </thead>

        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.CD_OBJETO</td>
                    <td>@item.CD_SITUACAO</td>
                    <td>@item.DSC_SITUACAO</td>
                </tr>
            }

        </tbody>

    </table>
</div>*@
